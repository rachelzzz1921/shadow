# Shadow — Agent 入口（根指针）

→ **请从 [`shadow-corpus/AGENTS.md`](shadow-corpus/AGENTS.md) 开始。**

→ **GitHub Pages：** 改 `docs/` 或 Visual 预览后及时 `npm run board:publish` 并 push `main` — [`.cursor/rules/github-pages-deploy.mdc`](.cursor/rules/github-pages-deploy.mdc)
