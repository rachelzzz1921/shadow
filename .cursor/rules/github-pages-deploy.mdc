---
description: docs/ 站点架构与 GitHub Pages 部署 — 详见 docs/SITE.md
alwaysApply: true
---

# GitHub Pages — 及时推送

对外 Demo 与团队看板在 `docs/`（分支 `main`）。**架构、Intake 映射、页面分工 → [`docs/SITE.md`](docs/SITE.md)**。

## 页面分工（速记）

| 路径 | 用途 |
|------|------|
| `/` | 对外首页 `index.html` |
| `/generate.html` | 唯一采集 + API 七年入口 |
| `/demo.html` | 七年阅读器 |
| `/pitch.html` | 路演 |
| `/board.html` | 团队看板（`board:publish` 生成） |

兼容跳转：`intake.html` → `generate.html`，`demo-hub.html` → `/`。

## 何时必须 push

改动 `docs/`、visual draft/registry、`site.board.json`、看板构建脚本后，同一会话内构建并 push（除非用户明确不要）。

## 标准流程

```bash
npm run board:publish
git add docs shadow-corpus .github package.json .cursor/rules
git commit -m "…"
git push origin main
```

## 禁止

- 只改本地 `docs/` 不 push却声称预览已更新
- 跳过 `board:publish` / `build:visual-demo` 推送过期 `board-data.json` 或 layout JSON
- 新建 `demo-live.html` / 独立 `intake.html` 对外链接（兼容跳转除外）
