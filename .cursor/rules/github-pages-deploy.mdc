---
description: docs/ 或 Visual 预览变更后及时构建并 push，保持 GitHub Pages 在线
alwaysApply: true
---

# GitHub Pages — 及时推送

团队看板与复读线 Demo 部署在 `docs/`（GitHub Pages，分支 `main`）。**本地改完不等于队友能看见**——必须 push 后 Pages 才更新。

## 何时必须 push

改动以下任一内容后，**同一会话内**完成构建、提交、推送到 `main`（除非用户明确说不要 push）：

- `docs/`（demo、看板、`demo-layouts-v1.json`、`visual-assets/`）
- `shadow-corpus/visual/scenes/fuxduxian/draft/`
- `shadow-corpus/visual/registry/packs/`
- `shadow-corpus/06-task-progress/site.board.json` / `site.config.json`
- 看板构建脚本或 `board-site.template.html`

## 标准流程

```bash
npm run board:publish          # 含 build:visual-demo
git add docs shadow-corpus .github package.json
git commit -m "…"
git push origin main
```

仅改 visual draft、未改 registry 时，至少：

```bash
npm run build:visual-demo
git add docs shadow-corpus/visual
git commit -m "…"
git push origin main
```

## 预览 URL（`site.config.json` → `githubPagesBase`）

- 看板：`/`
- 叙事 Demo：`/demo.html`
- Phaser v1：`/demo-phaser.html`

push 后在 commit 信息或回复里附上上述 Pages 链接，方便人审。

## 禁止

- 只改本地 `docs/` 不 push，然后声称「预览已更新」
- 跳过 `board:publish` / `build:visual-demo` 直接 push 过期的 `board-data.json` 或 layout JSON
